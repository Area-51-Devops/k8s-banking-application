apiVersion: apps/v1
kind: Deployment
metadata:
  name: service-discovery
  namespace: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: service-discovery
  template:
    metadata:
      labels:
        app: service-discovery
    spec:
      containers:
        - name: service-discovery
          image: area51devops/banking-app-service-discovery:latest
          ports:
            - containerPort: 3009
          envFrom:
            - configMapRef:
                name: service-discovery-config
          startupProbe:
            httpGet:
              path: /health
              port: 3009
            initialDelaySeconds: 5
            failureThreshold: 30
            periodSeconds: 5
          readinessProbe:
            httpGet:
              path: /health
              port: 3009
            initialDelaySeconds: 10
            periodSeconds: 5
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /health
              port: 3009
            initialDelaySeconds: 30
            periodSeconds: 10
            failureThreshold: 3

---
apiVersion: v1
kind: Service
metadata:
  name: service-discovery-service
  namespace: backend
spec:
  selector:
    app: service-discovery
  ports:
    - port: 3009
      targetPort: 3009
  type: ClusterIP
 
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: service-discovery-config
  namespace: backend
data:
  PORT: "3009"
